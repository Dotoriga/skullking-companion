# TODO - Skull King Companion

## Prochaines étapes

- [ ] Vérifier la compilation TypeScript (`npx tsc --noEmit`)
- [ ] Configurer EAS (`eas login` + `eas build:configure` + `eas.json` avec `buildType: "apk"`)
- [ ] Builder l'APK (`eas build --platform android --profile preview`)
